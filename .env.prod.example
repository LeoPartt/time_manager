# ========================================
# PRODUCTION CONFIGURATION
# ========================================
# These are used by compose.prod.yaml and CI/CD pipelines
# In production, use proper secrets management (Vault, AWS Secrets Manager, etc.)
# Copy this file to .env and fill in your actual values
# DO NOT commit the .env file with real credentials!

APP_NAME=Time Manager

# Database Configuration
DB_NAME=timemanager_prod
DB_USER=timemanager_user
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD
DB_HOST=db
DB_PORT=5432

# Server Configuration
SERVER_PORT=8080
ACTUATOR_PORT=8080

# Security Configuration
# IMPORTANT: Generate a strong random JWT secret (e.g., openssl rand -base64 32)
JWT_SECRET=CHANGE_ME_GENERATE_RANDOM_SECRET_32_CHARS_MIN
ADMIN_PASSWORD=CHANGE_ME_STRONG_ADMIN_PASSWORD

# CORS Configuration (optional - only needed if exposing web frontend)
# CORS_ALLOWED_ORIGINS=https://yourdomain.com

# Mail Configuration (Mailpit for email testing)
MAIL_HOST=mailpit
MAIL_PORT=1025
# MAIL_USER=Username  # Optional, for authenticated SMTP
# MAIL_PASSWORD=Password  # Optional, for authenticated SMTP